//C++ Program to Swap Two Numbers

#include <iostream>

using namespace std;

int main(){
    
    int a,b;
    cin >> a;
    cin >> b;

    swap(a,b);

    cout << "value of A: " << a << "value of B: " << b;
    return 0;
}